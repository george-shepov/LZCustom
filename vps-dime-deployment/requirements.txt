# LZ Custom - VPS Dime Deployment Requirements
# Complete feature set with AI capabilities

# Core Flask Framework
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7

# Database
sqlite3  # Built into Python

# Async HTTP for AI services
aiohttp==3.9.1
aiofiles==23.2.0

# AI Services (Optional - only install if using AI)
openai==1.3.7  # For OpenAI GPT integration
anthropic==0.7.8  # For Claude integration

# Security and Authentication
bcrypt==4.1.2
PyJWT==2.8.0

# Data Processing
pandas==2.1.4  # For analytics and data export
python-dateutil==2.8.2

# Environment Configuration
python-dotenv==1.0.0

# Production Server
gunicorn==21.2.0  # WSGI server for production
gevent==23.9.1  # Async support for gunicorn

# Monitoring and Logging
psutil==5.9.6  # System monitoring

# Image Processing (Optional)
Pillow==10.1.0  # For image optimization

# Email Support (Optional)
smtplib  # Built into Python
email-validator==2.1.0

# Development Tools (Optional)
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
flake8==6.1.0

# Note: Some packages are optional based on features enabled
# Minimal installation only needs Flask, Flask-CORS, aiohttp
# AI features require openai and/or anthropic packages
# Production deployment benefits from gunicorn and gevent
